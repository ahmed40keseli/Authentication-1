<!DOCTYPE html><!--Bu bildirim, belge türünü ve HTML sürümünü (bu durumda HTML5) tanımlar. -->
<html lang="en"><!-- HTML belgesinin açılış etiketi. Bu langözellik İngilizce için "en" olarak ayarlanmıştır. -->

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>home page</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-body-tertiary bg-dark">
    <div class="container-fluid"> <!--Düzen için Bootstrap'in konteyner sınıfını ( ) kullanır -->
      <a class="navbar-brand" href="#">home page</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="#">Home</a>
          </li>
          <%if (status=="ok" ){%> <!--Bu bloklar, içeriği 'durum' değişkeninin değerine göre koşullu olarak işler.  -->
            <li class="nav-item"><a class="nav-link" href="/profile">profile</a></li>
            <!--'Durum'a göre koşullu olarak görüntülenen düğmeler vardır. -->
            <li class="nav-item"><a class="nav-link" href="/logout">logout</a></li>
            <!--'Durum'a göre koşullu olarak görüntülenen düğmeler vardır. -->
            <%}else{%>
              <li class="nav-item"><a class="nav-link" href="/register">register</a></li>
              <!--'Durum'a göre koşullu olarak görüntülenen düğmeler vardır. -->
              <li class="nav-item"><a class="nav-link" href="/login">login</a> </li>
              <!--'Durum'a göre koşullu olarak görüntülenen düğmeler vardır. -->
              <%}%>
        </ul>
      </div>
    </div>
  </nav>
  <div class="container my-5"><!--Düzen için Bootstrap'in konteyner sınıfını ( ) kullanır -->
    <div class="card" style="width: 100%;">
      <div class="card-body">
        <%if (status=="ok" ){%>
          <h5 class="card-title">welcome <%= user.email%>
          </h5>
          <%}else{%>
            <h5 class="card-title">welcome to the site</h5>
            <%}%>
              <h6 class="card-subtitle mb-2 text-body-secondary">Card subtitle</h6>
              <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's
                content.</p>
              <%if (status=="ok" ){%>
                <button class="btn btn-primary"><a href="/profile"
                    style="color:white; text-decoration:none">profile</a></button>
                <button class="btn btn-primary"><a href="/logout"
                    style="color:white; text-decoration:none">logout</a></button>
                <%}else{%>
                  <button class="btn btn-primary"><a href="/login"
                      style="color:white; text-decoration:none">login</a></button>
                  <button class="btn btn-primary"><a href="/register"
                      style="color:white; text-decoration:none">register</a></button>
                  <%}%>


      </div>
    </div>
  </div>
</body>

</html>